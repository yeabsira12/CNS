{
  "entities": {
    "User": {
      "title": "User",
      "description": "User profile information synchronized from federated identity provider.",
      "type": "object",
      "properties": {
        "displayName": { "type": "string" },
        "email": { "type": "string" },
        "photoURL": { "type": "string" },
        "lastLogin": { "type": "string", "format": "date-time" }
      },
      "required": ["email"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "Public user profiles accessible by the user themselves."
    }
  }
}
